type Registro @model @auth(rules: [{ allow: owner, ownerField: "owner" }]) {
  id: ID!
  tipo: String!
  valor: Float!
  descricao: String!
  data: String!
  createdAt: String!
  updatedAt: String!
  owner: String!
}
